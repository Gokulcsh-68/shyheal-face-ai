/**
 * SkyHeal AI — Scan Screen Controller
 */
class ScanScreenController {
    constructor(app) { this.app = app; }
    // Managed by main.js ScanManager
}
window.ScanScreenController = ScanScreenController;
